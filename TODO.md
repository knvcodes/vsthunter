# TODO - Plugin Slug URL Implementation

- [ ] 1. Add `slug` field to content schema in `src/content.config.ts`
- [ ] 2. Add `slug` to all 5 markdown files
- [ ] 3. Update `getStaticPaths()` in `src/pages/plugin/[slug].astro` to use `slug` field
- [ ] 4. Update `PluginCard.astro` link to use `slug` field
